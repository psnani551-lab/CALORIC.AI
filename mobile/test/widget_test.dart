import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Core smoke test', (WidgetTester tester) async {
    // Test disabled as it requires SharedPreferences mocking
    expect(true, true);
  });
}
